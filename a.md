

| Paper | Conf. / Journal | Year | Dataset | mAP Novel (LVIS / others) | mAP Known (LVIS / others) | Main Limitations |
| --- | --- | --- | --- | --- | --- | --- |
| Sylph: A Hypernetwork Framework for Incremental Few-Shot Object Detection | CVPR | 2022 | COCO, **LVIS v1** | **LVIS rare (APr, novel)**: 13.9 | **LVIS base (approx.)**: 22.3  (APc 19.0, APf 25.5) | Few-shot **supervised** (needs K labeled examples per novel class); heavy meta-training with FCOS + hypernetwork; not training-free; no text or VLM priors; still significantly lower LVIS rare AP than your training-free CoFM (17.4) even with extra training. |
| Incremental Few-Shot Object Detection via Simple Fine-Tuning Approach (iTFA) | IEEE TNNLS | 2023 | VOC, COCO, **LVIS v1** | **LVIS rare (APr, novel)**: 18.1 | **LVIS base (approx.)**: 25.5 (APc 21.0, APf 29.9) | Incremental few-shot **fine-tuning**; requires full base-detector training + extra fine-tuning for each novel step; still supervised on novel classes; no zero-shot ability; no VLM semantic guidance; more complex training schedule than your CoFM pipeline and higher computational cost. |
| **Revisiting Few-Shot Object Detection with Vision-Language Models** | **NeurIPS 2024 (Datasets & Benchmarks)** :contentReference[oaicite:4]{index=4} | 2024 | COCO, **LVIS v1**, nuImages. LVIS-base = frequent+common; LVIS-rare = novel. :contentReference[oaicite:5]{index=5} | **LVIS (10-shot Detic + Pseudo-Negatives):** APr = **19.2** on rare (novel) classes. :contentReference[oaicite:6]{index=6} | **LVIS base:** APf = 34.6, APc = 33.2 (≈ **33.9** AP on known classes on average). :contentReference[oaicite:7]{index=7} | • **Still few-shot supervised:** they fine-tune Detic with a small number of labeled rare-class boxes + federated losses and pseudo-negatives; not training-free like our CoFM pipeline.<br>• **Detic already CLIP-pretrained on LVIS labels:** VLM has seen LVIS classes during training, so higher accuracy partly comes from label overlap, not from true “novel” generalization.<br>• **LVIS metric is (APf, APc, APr), not our mAP@[0.5:0.95] known/novel NOD split:** direct comparison with our LVIS-NOD table is not strictly fair; their goal is improving FSOD benchmarks, not pure NOD.<br>• **Focus shifted to loss design (FedLoss / InvFedLoss) instead of modular multi-model cooperation:** does not explore cooperative VFM+VLM combinations like CLIP+SAM+DINOv2, so it doesn’t directly address our main contribution (cooperative foundational models). |






























| # | Paper (short name) & Venue/Year | Learning type (how they train) | LVIS setting used | LVIS metrics reported (official) | How “novel” vs “known” are measured | Main limitations (only what papers explicitly say or clearly imply) |
|---|---------------------------------|---------------------------------|-------------------|-----------------------------------|--------------------------------------|---------------------------------------|
| 1 | **ViLD – “Open-vocabulary Object Detection via Vision and Language Knowledge Distillation” (Gu et al.) – CVPR 2022** | **Supervised OVD with KD:** trains a Faster R-CNN detector on **LVIS base categories with full box masks**, and distills CLIP/ALIGN features for open-vocab classification. It is a *fully supervised detector* on base classes; novel classes appear only through vision–language model text. :contentReference[oaicite:0]{index=0} | **OV-LVIS benchmark** (train on LVIS base, test on all LVIS categories). Numbers below are **as re-reported in OADP and SAS-Det**. :contentReference[oaicite:1]{index=1} | **LVIS-OVD, RN50-FPN, Faster R-CNN (Table 2 in SAS-Det/OADP):** APr (rare/novel) = **16.7**, APc (common) = **26.5**, APf (frequent) = **34.2**, AP (all) = **27.8**. :contentReference[oaicite:2]{index=2} | LVIS is split into **rare (novel)** vs **common + frequent (base)**. APr is the score on rare classes; APc & APf are known/base regions; AP is the overall AP on all classes. :contentReference[oaicite:3]{index=3} | OADP and later works explicitly point out two issues for ViLD: (1) **knowledge extraction by simple crops may cut object parts or add background noise**, so CLIP features are not object-pure; (2) training is **very expensive (16× schedule + LSJ augmentation)** compared to later methods. :contentReference[oaicite:4]{index=4} |
| 2 | **OADP – “Object-Aware Distillation Pyramid for Open-Vocabulary Object Detection” (Wang et al.) – CVPR 2023** | **Supervised OVD (KD-based) with pseudo labels only for novel PLs in some settings**, but the LVIS-OVD results we use are standard OVD **trained on LVIS base categories with full supervision + CLIP teacher**, using their Object-Aware Knowledge Extraction (OAKE) + Distillation Pyramid. :contentReference[oaicite:5]{index=5} | **OV-LVIS benchmark** (same LVIS-OVD setting as ViLD). :contentReference[oaicite:6]{index=6} | **LVIS-OVD (Table 3 in OADP, RN50-FPN, Faster R-CNN):** APr = **21.9**, APc = **28.4**, APf = **32.0**, AP (all) = **28.7**. :contentReference[oaicite:7]{index=7} | Same LVIS split as ViLD: **APr (rare/novel)** vs **APc, APf (common/frequent known)** and AP over all categories. :contentReference[oaicite:8]{index=8} | The paper emphasizes improvements over ViLD, but it **still relies on fully labeled LVIS base categories and CLIP teacher features**, and uses a multi-stage KD pipeline. They also mention that OVD methods like theirs are not directly comparable to caption-based methods that use extra large datasets, and that **training cost is still significant** compared to simple training-free pipelines. :contentReference[oaicite:9]{index=9} |
| 3 | **SAS-Det – “Taming Self-Training for Open-Vocabulary Object Detection” (Zhao et al.) – CVPR 2024** | **Pseudo-label / self-training OVD (semi-supervised-style):** detector is initialized on **base categories with full supervision**, then **pseudo labels (PLs) for novel categories** are generated using CLIP and a teacher detector, and self-training is performed with a split “SAF head” (open branch + closed branch). This is *not unsupervised*: base classes still have human labels; novel classes are trained via PLs. :contentReference[oaicite:10]{index=10} | **LVIS-OVD** (called LVIS-OVD in the paper; same standard split: 337 rare as novel, 866 common+frequent as base). :contentReference[oaicite:11]{index=11} | **LVIS-OVD (Table 2, RN50-C4, 2× + default schedule):** APr = **20.9**, APc = **26.1**, APf = **31.6**, AP (all) = **27.4**. There is also a stronger RN50x4-C4 variant with APr = **29.1**, APc = **32.4**, APf = **36.8**, AP = **33.5**. :contentReference[oaicite:12]{index=12} | As above: **rare = novel**, **common+frequent = base**. They explicitly state APr (rare), APc (common), APf (frequent), AP (all) as metrics. :contentReference[oaicite:13]{index=13} | The authors explicitly note that: (1) **their LVIS APr is much higher than ViLD but APf (base/frequent) is lower**, and they attribute this to **shorter training (2× vs 16×) and weaker augmentation (default vs LSJ)**; (2) they show that many self-training update strategies (e.g., EMA teacher, frequent teacher updates) **cause training instability and big drops in APnovel**, so the method is sensitive to pseudo-label quality and teacher-update schedule. :contentReference[oaicite:14]{index=14} |
| 4 | **PB-OVD – “Open Vocabulary Object Detection with Pseudo Bounding-Box Labels” (Gao et al.) – ECCV 2022** | **Pseudo-label–based OVD (semi-supervised-ish):** they generate **pseudo bounding boxes from large image–caption datasets (COCO Captions, Visual Genome, SBU, etc.) using a VL model**, then use those pseudo boxes as **training supervision** for an open-vocab detector. Base classes use pseudo or human boxes; LVIS is used mainly to test cross-dataset generalization. :contentReference[oaicite:15]{index=15} | **COCO-trained detector evaluated on LVIS** to measure cross-dataset generalization. LVIS itself is **not used as fully labeled training data** in their main OVD setup; LVIS is an unseen target dataset. :contentReference[oaicite:16]{index=16} | On **COCO-OVD** they report: mAP50 on novel = **29.1** (COCO Cap only) or **30.8** (COCO Cap + VG + SBU), mAP50 on base = **44.4 / 46.1**, mAP50 all = **40.4 / 42.1**. :contentReference[oaicite:17]{index=17} For **LVIS**, they only report **overall AP improvement (+2.8 AP) over the SOTA baseline** in cross-dataset evaluation; the paper does **not** break LVIS into separate mAP for novel vs known categories in the same APr/APc/APf style as LVIS-OVD works. :contentReference[oaicite:18]{index=18} | “Novel vs known” is clearly defined on **COCO** (novel/base). For LVIS, they treat it as a target dataset for **generalization** and only report **overall AP gain** vs the baseline, **not** separate rare/common/frequent numbers. So **per-class LVIS novel/known AP values are not available in the paper.** :contentReference[oaicite:19]{index=19} | The authors themselves highlight that: (1) pseudo-labels come from image-caption VL models and **can be biased or noisy**, which can transfer bias into the detector; (2) LVIS/Objects365 experiments show only **moderate AP (they talk about +2.8 AP gain)**, so truly universal detection across many unseen categories is still far from solved; and (3) they explicitly mention the risk that pseudo labels **reflect biases in the caption data and VL model**, which is a limitation they discuss. :contentReference[oaicite:20]{index=20} |
| 5 | **RNCDL – “Learning to Discover and Detect Objects” (Fomenko et al.) – 2022 (baseline re-reported in PANDAS)** | **Unsupervised novel-class discovery + supervised detection:** they **train on labeled base classes**, then perform **novel class discovery (clustering) using self-supervised features and pseudo labels**, and finally fine-tune the detector to detect both base and discovered novel classes. In PANDAS, RNCDL is treated as a **Novel Class Discovery (NCD) detection baseline** for **COCOLVIS**, combining COCO and LVIS. :contentReference[oaicite:21]{index=21} | **COCOLVIS benchmark** (COCO images + LVIS categories): train on COCO with base labels, then evaluate on LVIS categories (base + novel). This uses LVIS category definitions for evaluation; the numbers below are from **PANDAS Table 3**. :contentReference[oaicite:22]{index=22} | From **PANDAS Table 3 (RNCDL, box-supervised variant)**: mAP_base = **39.9**, mAP_novel = **6.2**, mAP_all = **8.9**, mAP_f (frequent) = **11.7**, mAP_c (common) = **7.1**, mAP_r (rare) = **6.5** on COCOLVIS. :contentReference[oaicite:23]{index=23} | Here “novel vs known” is defined through **LVIS hierarchy on COCOLVIS**: LVIS rare classes act as novel, common+frequent as base. PANDAS explicitly states this split and reports base/novel/all, plus f/c/r. :contentReference[oaicite:24]{index=24} | PANDAS describes RNCDL’s drawbacks: **low novel-class mAP (6.2) compared to base (39.9)**, showing that the NCD pipeline **has difficulty discovering fine-grained long-tail LVIS categories**; and **it is sensitive to the choice of prior (uniform vs long-tailed) and clustering**, with performance varying significantly across those variants. :contentReference[oaicite:25]{index=25} |
| 6 | **PANDAS – “PANDAS: Pseudo-label-based Anchor-free Novel-class Discovery for Object Detection” (2024) – arXiv / later** | **NCD-style unsupervised discovery + detection:** PANDAS improves on RNCDL by using an **anchor-free pseudo-label generation and clustering pipeline** to discover novel classes on COCOLVIS, then trains an object detector to handle both base and discovered novel classes. Base classes still have labels; novel classes are discovered in an unsupervised way. :contentReference[oaicite:26]{index=26} | **COCOLVIS benchmark** same as RNCDL (COCO images, LVIS categories). They train on labeled base + unsupervised novel, and evaluate on LVIS category split (novel/known). :contentReference[oaicite:27]{index=27} | From **PANDAS Table 3 (“PANDAS (Ours)” box-only row)**: mAP_base = **45.6**, mAP_novel = **8.7**, mAP_all = **11.5**, mAP_f = **14.7**, mAP_c = **9.2**, mAP_r = **10.4**. :contentReference[oaicite:28]{index=28} | Same LVIS-style split on COCOLVIS as RNCDL: base vs novel determined by LVIS frequencies; they report base/novel/all and f/c/r. :contentReference[oaicite:29]{index=29} | The authors clearly state that although PANDAS **significantly improves novel-class mAP over RNCDL (8.7 vs 6.2)**, the **absolute novel mAP is still low** compared to fully supervised OVD methods, and that **discovering fine-grained long-tail LVIS classes remains challenging**. They also highlight that **performance depends on the quality of pseudo labels and clustering**, which is a core limitation of NCD-style approaches. :contentReference[oaicite:30]{index=30} |



